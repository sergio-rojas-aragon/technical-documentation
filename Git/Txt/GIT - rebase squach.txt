git rebase squach es que agrega dos commit y los choca para que se mezclen

toma los 4 ultimos commits
git rebase -i HEAD~4

en el editor se selecciona cual se le va a hacer el squach
para salir precionar escape
:wq

luego pedira cambiar el mensaje de los dos commits que se van a unir
